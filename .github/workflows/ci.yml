name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
        include:
          # Also install the optional `google` extra on one lane so the
          # google.genai SDK-pin tests in test_gemini_sdk_response.py run
          # for real instead of being skipped via importorskip.
          - python-version: "3.11"
            extras: "dev,google"

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[${{ matrix.extras || 'dev' }}]"

    - name: Run ruff
      run: |
        ruff check src tests

    - name: Run mypy type checking
      run: |
        mypy src/multi_agent_harness

    - name: Run unit tests
      run: |
        pytest tests/unit -v --cov=src/multi_agent_harness --cov-report=xml

    - name: Run integration tests
      run: |
        pytest tests/integration -v

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      if: matrix.python-version == '3.11'
      with:
        file: ./coverage.xml
        fail_ci_if_error: false

  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Lint with ruff
      run: |
        ruff check src tests --output-format=github
